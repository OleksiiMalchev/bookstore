server:
  port: 8080
spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        generate_statistics: false
        show_sql: true
  flyway:
    enabled: true
    locations: classpath:db.migration
    outOfOrder: true




